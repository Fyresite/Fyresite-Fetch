{
  "name": "@fyresite/fetch",
  "version": "1.0.7",
  "description": "Uses whatWG-fetch to provide an easier way to send api requests",
  "main": "bin/index.js",
  "dependencies": {
    "whatwg-fetch": "^2.0.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "babel src --presets babel-preset-es2015 --out-dir bin",
    "build": "babel src --presets babel-preset-es2015 --out-dir bin"
  },
  "author": "Fyresite",
  "license": "ISC",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/Fyresite/Fyresite-Fetch"
  },

  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
